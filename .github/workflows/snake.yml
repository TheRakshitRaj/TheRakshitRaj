name: Generate Snake
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: Platane/snk@v3
        with:
          github_user_name: TheRakshitRaj
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
      - uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

5. Click **"Commit changes"** → **"Commit directly to main"** → **Commit** ✅

---

**Step 3 — Run the workflow manually**

1. Go to the **Actions** tab of your repo
2. You'll see **"Generate Snake"** in the left sidebar — click it
3. Click the **"Run workflow"** dropdown on the right → **"Run workflow"** ▶️
4. Wait ~30 seconds — you'll see a green ✅ when it's done

---

**Step 4 — Verify it worked**

After the green checkmark, go to:
```
https://github.com/TheRakshitRaj/TheRakshitRaj/tree/output
